name: secret-test
on: push

jobs: 
  testing-secrets:
    runs-on: ubuntu-latest
    steps: 
      - name: Use secrets
        run: |
         echo " ${{secrets.MY_NAME}}"
         echo " ${{secrets.MY_ROLE}}"