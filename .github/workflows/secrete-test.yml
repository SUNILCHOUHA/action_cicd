name: secret-test
on: push

jobs: 
  testing-secrets:
    runs-on: ubuntu-latest
    steps: 
      - name: Use secrets
        run: |
         echo "My Name : ${{secrets.MY_NAME}}"
         echo "My Role : ${{secrets.MY_ROLE}}"